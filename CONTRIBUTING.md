# 参与贡献

## 提问题

用 GitHub Issues 描述三件事：你期待发生什么、实际发生了什么、怎么复现。

如果是「该触发时没触发」或「不该触发却触发了」，**把你当时说的原话一并写上**——
触发词靠真实说法迭代，转述过的说法参考价值会掉很多。

如果是归集结果不对，附上 `--dry-run` 的输出，以及被误判的那几行记忆文件长什么样。

## 改代码

1. Fork 后建分支，改完提 PR
2. 自测三件事：
   - `python3 scripts/harvest.py --dry-run --sample 60` —— 命中量是否仍在合理量级
   - `python3 scripts/harvest.py` 连跑两遍 —— 输出应完全一致
   - 打开生成的 `todos.html`，勾几条 → 导出 → `--apply-checked` 写回 —— 勾选应能被记住
3. PR 里说明改了什么、影响哪部分行为

## 本地约定

- **纯标准库**，不引第三方依赖（`check_deps` 会拦）
- **页面产物零外部依赖**：CSS / JS 全内联，不引 CDN、不引图表库
- **不预设用户目录**：配置驱动，路径不写死
- **清单里不得出现密码 / token 明文**
- 改过待办识别或归属逻辑，请同时更新 `references/口径说明.md`；
  改了触发词或描述，请跑一次触发词评估集
